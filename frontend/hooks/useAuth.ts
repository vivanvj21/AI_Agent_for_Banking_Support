// Re-export useAuth from AuthProvider for convenient import paths.
// This avoids deep relative paths like '../../../../components/providers/AuthProvider'
export { useAuth } from '@/components/providers/AuthProvider';
