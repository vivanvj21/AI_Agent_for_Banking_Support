// Auth layout — minimal wrapper with no sidebar/topbar.
// The (auth) route group uses a clean full-screen layout.
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
